(module Main imports ()
(vars ())
(functions (
  (function main()      (begin 
      (let %0 10)
      (let %1 %0)
      %1)

  (function init()      (begin 
      (let %0 0)
      (let %1 %0)
      %1)

))
