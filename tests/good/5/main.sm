(module Main imports (StdLib))

(vars (
  (let x 0)
  (let y 0)
))

(functions (

  # this function will NO LONGER cause a runtime exception
  # we now have mutable local variables as well as fn parameters
  (function fail (a b)
    (begin
      (set! a 23)
      a  
    )
  )

  (function main ()
    (begin
      (let seven 7)
      (fail x y)
    )
  )

))
