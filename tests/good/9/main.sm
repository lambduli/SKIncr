(module Main imports (StdLib))

(vars (
  (let ALIST (list 1 2 3 4 5))
  # (let BLIST (list 10 9 8 7 6))
))

(functions (

  (function polyvar (a b ...c)
    c
  )

  (function main ()
    (begin
      (let a (list 0 1 0 1 0 1))
      (write! "a: " a)

      (let b (polyvar 0 1 2 3 4 5))
      (write! "b:" b)

      (let c (polyvar "a" "b" "c" "d"))
      (write! "c:" c)

      (let d (head c))
      (write! "d:" d)

      (let lst (list 23 24 25 26))
      (write! "lst: " lst)
      
      (write! "ALIST: " ALIST)
      (write! "ALIST size= " (size ALIST))

      (let e (head (tail ALIST)))
      (write! "e: " e)
      0
    )
  )

))
