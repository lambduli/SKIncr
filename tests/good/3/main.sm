(module Main imports (StdLib))

(vars (
  (let two 2)
  (let three 3)
  (let cond (< two three))
  (let x (if cond 0 1)) # this is just to test that the if works, no real reason behind it
  (let shouldContinue True)
))

(functions (
  (function main ()
    (foo))

  (function not (bool)
    (if bool False True))

  # no idea what this function does
  (function foo ()
    (begin
      (let a 23)
      (while shouldContinue
        (begin
          (let one 1)
          (let y (+ one x))
          (set! x y)
          (let eq (= x a))
          (set! shouldContinue (not eq))
        )
      )
      x
    )
  )
))
