(module Main imports (StdLib))

(vars ())

(functions (
  (function main ()
    (begin
     (let x 1)
     (let y x)
     (let w y)
     (while (< x 10)
       (begin
 	 (set! x (+ x 1))
	 (if (< x 11) (set! z 0) (set! w 0))
	 (set! w (+ y x)))
     )
     x ## note that if we omit x we crash :-(, the checker fails to check!
    )
  )
 )
)
