(module Lib imports (StdLib))

(vars (
  (let a 0)
  (let b 0)
))

(functions (
  (function someFun ()
    (begin
      (write! a)
      (set! a 23)
      (write! a)
      (write! b)
      (set! b (read!))
      (write! b)
      (let exitcode 0)
      exitcode
    )
  )
))
