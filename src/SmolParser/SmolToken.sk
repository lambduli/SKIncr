module SmolToken ;

base class Token {
  children  = Identifier(String)
            | Keyword(String) // module, imports, vars, functions, loop, begin, function, const, let
            | OpenParen()
            | CloseParen()
            | Number(Int)
            | Operator(Op)
}

module end ;